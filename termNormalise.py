'''
define the normalisation of term
in this project, we just normalise a term to itself as a naive implementation
'''


def normalise(token):
	'''
	normalise a token to its standard concept
	so far I just normalise a token to itself
	'''

	return token